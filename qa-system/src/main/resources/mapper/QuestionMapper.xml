<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="top.hyzhu.qasystem.mapper.QuestionMapper">
    <insert id="insertQuestion" parameterType="Question">
        INSERT INTO question (user_id, title, content, create_time)
        VALUES (#{userId}, #{title}, #{content}, #{createTime})
    </insert>

    <select id="getLastInsertedQuestion" resultType="Question">
        SELECT * FROM question ORDER BY id DESC LIMIT 1
    </select>
</mapper>